pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
        stage('Build') {
            steps {
                bat 'set'
            }
        }
    }
}
